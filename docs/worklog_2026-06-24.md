<!-- SPDX-License-Identifier: CC-BY-4.0 -->

# Worklog - 2026-06-24

## Validation and Tracker reproducibility updates

Today the repository validation evidence was reorganized and expanded for HardwareX-style review.

## Completed

- Added representative validation videos to `validation/media/` through Git LFS.
- Confirmed the representative videos are real LFS files after pulling locally.
- Added full Tracker-linked joint-motion videos next to their `.trk` files in `validation/tracker_files/joint_motion/`.
- Added Tracker-linked kinematics `.jpeg` images next to their `.trk` files in `validation/tracker_files/kinematics/`.
- Normalized one kinematics Tracker path that pointed to an old local `Pictures/Camera` location.
- Confirmed the tested joint-motion `.trk` opens directly with its linked `.mp4` without manual file search.
- Confirmed the tested kinematics `.trk` opens directly with its linked `.jpeg` without manual file search.
- Updated `.gitattributes` to keep Markdown indexes inside `validation/media/` as normal text, while keeping media in Git LFS.
- Updated `validation/validation_data_index.md` to explain the uploaded Tracker-linked media.
- Added `paper/tables/validation_summary.md` as a manuscript-facing validation evidence table.

## Key commits

| Commit | Summary |
|---|---|
| `e93ec27` | Add representative validation videos. |
| `c817156` | Update media index after representative video upload. |
| `e8cd64b` | Update validation data index after video upload. |
| `c74422c` | Exclude validation media Markdown from LFS. |
| `b0d571d` | Add Tracker-linked validation media. |
| `972870c` | Update validation data index for Tracker-linked media. |
| `06ad5b1` | Add validation summary table. |

## Current status

- Repository is synchronized on `main`.
- Working tree was clean after pulling the documentation updates.
- Tracker reproducibility is confirmed for at least one joint-motion project and one kinematics project.
- Remaining local raw media can stay excluded locally unless needed later.
