<!-- SPDX-License-Identifier: CC-BY-4.0 -->

# Tasks for 2026-06-25

## Priority 1 - Validation package closure

- [ ] Run a clean local check of Tracker projects from `validation/tracker_files/joint_motion/` and `validation/tracker_files/kinematics/`.
- [ ] Verify that at least one `.trk` per joint J1-J5 opens with its linked `.mp4` without asking for a file path.
- [ ] Verify that at least one kinematics `.trk` for P1-P5 opens with its linked `.jpeg` without asking for a file path.
- [ ] Review `paper/tables/validation_summary.md` and adjust wording for the final manuscript.
- [ ] Decide whether the representative videos in `validation/media/` are still needed now that full Tracker-linked media exist under `validation/tracker_files/`.
- [ ] Add a short reviewer note explaining that cloning requires Git LFS: `git lfs install` and `git lfs pull`.

## Priority 2 - HardwareX manuscript tables and figures

- [ ] Convert `paper/tables/validation_summary.md` into a manuscript-ready table.
- [ ] Select final validation figures from `validation/joint_motion_tests/plots/` for the paper.
- [ ] Prepare a compact validation-results paragraph for J5 ramp, J5 trap, J1 step, perception and teleoperation.
- [ ] Prepare figure captions for validation videos/Tracker evidence if used as supplementary files.

## Priority 3 - Repository cleanup

- [ ] Run `git status --short` and confirm the working tree is clean.
- [ ] Run `git lfs ls-files` and confirm CAD, Tracker files and videos are under LFS.
- [ ] Run a quick README link/path check for validation-related files.
- [ ] Update `docs/publication_checklist.md` manually if needed, marking Tracker-linked media as uploaded and tested.
- [ ] Confirm `validation/media/media_index.md` and `validation/validation_data_index.md` match the final validation structure.

## Priority 4 - Mechanical/electronics pending items

- [ ] Continue the CAD/BOM review for remaining mechanical parts if v60 is still the working snapshot.
- [ ] Confirm pending physical details when the robot is accessible: TB6600 supply voltage, connector pin order, wire colors and electronics photos.
- [ ] Do not change firmware J5/B5 behavior until the physical robot is available for validation.

## Suggested start command

```bash
cd ~/assistbelle-arm-elevator-subsystem
git pull
git status --short
git lfs ls-files | grep -E "validation/tracker_files|validation/media|hardware/(cad|step|stl|drawings)"
```
